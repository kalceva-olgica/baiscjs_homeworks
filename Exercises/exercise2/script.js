function weightInChickens(input) {
  const weight = prompt("Enter your weight in kilograms");
  chickens = weight / 0.5;
  document.getElementById(
    "result-paragraph"
  ).textContent = `That weigrht equals ${chickens} chickens.`;
}

weightInChickens();

// 4 linii kod saat ipol dvoumenje kako se resava hahahaha
